package doc

import (
	"net/http"

	"lazyrag/core/common"
)

// WriterSegmentJob 占位实现，后续补全。

func Submit(w http.ResponseWriter, r *http.Request) { common.ReplyJSON(w, map[string]any{}) /* TODO */ }
func Get(w http.ResponseWriter, r *http.Request)    { common.ReplyJSON(w, map[string]any{}) /* TODO */ }
