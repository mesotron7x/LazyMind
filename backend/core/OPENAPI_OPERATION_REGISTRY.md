# Core OpenAPI text

text `core` text OpenAPI text：

- `Operation Registry + Reflect Schema Generator`
- text manual override text

## text

`core` text `Go + net/http + gorilla/mux`，text `routes.go`，text OpenAPI text query/body/response text `openapi_manual.go` text，text：

- text，DocumenttextUpdate
- query text handler text，text
- request/response text Go text，text

## text

text handler text，text：

1. Documenttext
2. text JSON text Go text schema
3. text `openapi_manual.go` text
4. text

## text

### 1. text

text `routes.go` text。

### 2. Operation Registry

`openapi_registry.go` text `openAPIOperation` text，text：

- `Method`
- `Path`
- `Summary`
- `Tags`
- `PathParams`
- `QueryParams`
- `Headers`
- `RequestBody`
- `Responses`

### 3. Reflect Schema Generator

text Go struct：

- text OpenAPI primitive
- struct text object schema
- slice/array text array schema
- map text object/additionalProperties schema
- `time.Time` text `string(date-time)`
- `json` tag text
- `omitempty` text/text/map text

### 4. Manual Override

`openapi_manual.go` text，text：

- `multipart/form-data`
- `application/octet-stream`
- `text/event-stream`
- ACL textResponse
- text
- text registry text legacy text

## text

`openapi_gen.go` text：

1. text swagger Document
2. merge manual spec
3. overlay registry spec
4. text router，text path/method text path params
5. text OpenAPI

text：

- `mergeOpenAPISpec`：text manual/base text
- `overlayOpenAPISpec`：text registry text path/method text manual text

## text

text，text。text OpenAPI text，text Go text，text：

- text（path/query/header/body）
- content-type
- ErrorResponse
- SSE / binary / multipart
- Defaulttext、text、text、text

text：

- text JSON text：text registry + Go struct text
- text：text manual override

## text

### text JSON text

1. text `routes.go` text
2. text `openapi_registry.go` text `openAPIOperation`
3. text query/path text，text struct，text tag text：
   - `path:"dataset"`
   - `query:"page_size"`
4. RequesttextResponsetext Go struct
5. text `openapi.json/openapi.yml`

### text

textUpload、Download、SSE、ACL text，text `openapi_manual.go` text override。

## text

text，text：

1. query/path text struct text，text handler text
2. request/response text struct
3. text content-type text
4. ErrorResponsetext
5. text map/text manual override

## text

text `openapi_manual.go`，text：

- textDocument
- text JSON text Go text
- text `openapi.yml` text
- Documenttext“text schema”text“text”

## text

text：

1. text legacy text `openapi_manual.go` text `openapi_registry.go`
2. text tag（text required、enum、minimum、maximum）
3. textErrorResponsetext
4. text registry text“text”text“text”
